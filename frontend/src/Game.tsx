import React from "react";

const Game: React.FC = () => {
    return (
        <div>
            <h2>Game Board</h2>
            {/* Board, Controls, and game state UI will go here */}
        </div>
    );
};

export default Game;